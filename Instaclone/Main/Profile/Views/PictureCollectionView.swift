import SwiftUI

struct PictureCollectionView: View {
    var images: [Image]
    
    var body: some View {
        VStack {
            //Hier kommen die Bilder hin (Collection)
            Text("")
        }
    }
}
